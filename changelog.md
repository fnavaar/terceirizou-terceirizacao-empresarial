## 2026-09-05

- [Adapta/Ethos] F3-T02 destravada e validada. Causa raiz do bloqueio era dupla: (1) regex da janela no hook `agendar_lead.js` usava `\\d` (escapado duplo) e nunca casava dígitos, então toda requisição válida retornava 400 "inicio deve usar o fuso"; (2) frontend chamava `fetch('/backend/v1/agendar-lead')` relativo ao domínio público, que responde 405 (rotas custom só existem no backend interno). Corrigidos: regex `\d` e `fetch(pb.baseUrl + '/backend/v1/agendar-lead')`. Skip v0.0.41 (4f60fb4), QA ok, publicado. Validação CA-3-001/002 no backend: não qualificado → 409 bloqueado (sem chamada calendar); qualificado em janela válida sem token → 503 pendente_configuracao (sem falso sucesso); fora da janela → 409. GREEN completo do agendamento depende de o champion fornecer o token OAuth do Google Calendar (GOOGLE_CALENDAR_ACCESS_TOKEN) — estado legítimo pendente_configuracao.

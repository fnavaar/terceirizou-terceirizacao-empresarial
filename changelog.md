## 2026-09-06

- [Adapta/Ethos] F3-T02 GREEN fechado com calendário real. Configuradas no Skip as secrets OAuth do Google Calendar (`GOOGLE_CALENDAR_ACCESS_TOKEN`, `REFRESH_TOKEN`, `CLIENT_ID`, `CLIENT_SECRET`) para a conta `financeiro@terceirizou.com.br`; Google Calendar API habilitada no projeto 200696676882. Prova real: `POST /backend/v1/agendar-lead` com lead sintético qualificado (07/09 10:00–10:30 America/Sao_Paulo) → 201 `scheduled`, evento `kdjet4js6i2ptoiq1pb92out68` criado no calendário e referência gravada no CRM (estado `agendado`, owner Henrique Tavano, próxima ação `aguardar_reuniao`); repetição → 200 `already_scheduled` (idempotência CA-3-002); lead não qualificado → 409 `bloqueado` `chamada_calendar:false` (CA-3-001). Nenhum token no Git. Commit 22965e3.
- [Adapta/Ethos] F3-T02: layout da tabela corrigido (coluna Ação sticky à direita, Qualificação visível) — v0.0.42.
- [Adapta/Ethos] F3-T02: causa raiz do "verifique a configuração" no teste humano identificada nos logs: as tentativas do champion chegavam ao backend com HTTP 401 (`The request requires valid record authorization token`) — token de sessão do navegador invalidadou após deploy (a chave JWT muda). Frontend agora refaz login automaticamente em 401 (authWithPassword) e mostra a mensagem real do backend — v0.0.43, hash `bfd1985`, publicado.
- [Adapta/Ethos] F3-T02: segundo erro do teste humano — "janela deve ter exatamente 30 minutos". Causa raiz: o frontend calculava `fim` com `new Date(Date.parse(inicio)+30min).toISOString().slice(0,19)` que devolve em UTC, rotulando como `-03:00`; diferença real virava 3h30. Corrigido calculando `fim` aritmeticamente mantendo o fuso `-03:00` (11:00→11:30). Validado localmente (1800000 ms = 30 min). — v0.0.44, hash `cce2a45`, publicado.
